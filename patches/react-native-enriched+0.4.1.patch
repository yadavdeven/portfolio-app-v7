diff --git a/node_modules/react-native-enriched/android/src/main/java/com/swmansion/enriched/textinput/EnrichedTextInputView.kt b/node_modules/react-native-enriched/android/src/main/java/com/swmansion/enriched/textinput/EnrichedTextInputView.kt
index 3badaa4..b31d9b5 100644
--- a/node_modules/react-native-enriched/android/src/main/java/com/swmansion/enriched/textinput/EnrichedTextInputView.kt
+++ b/node_modules/react-native-enriched/android/src/main/java/com/swmansion/enriched/textinput/EnrichedTextInputView.kt
@@ -75,6 +75,12 @@ class EnrichedTextInputView : AppCompatEditText {
   val parametrizedStyles: ParametrizedStyles? = ParametrizedStyles(this)
   var isDuringTransaction: Boolean = false
   var isRemovingMany: Boolean = false
+  // PATCH (atomic mentions): set in the text watcher's beforeTextChanged when a
+  // deletion removes a character that is inside a mention span. Lets the mention
+  // logic distinguish "edited the mention itself" (delete the whole mention)
+  // from "edited next to the mention" (e.g. removed the trailing space / typed
+  // adjacent — leave the mention intact).
+  var mentionContentDeleted: Boolean = false
   var scrollEnabled: Boolean = true
 
   val mentionHandler: MentionHandler? = MentionHandler(this)
@@ -154,7 +160,11 @@ class EnrichedTextInputView : AppCompatEditText {
     isHorizontalScrollBarEnabled = false
     isVerticalScrollBarEnabled = true
     gravity = Gravity.TOP or Gravity.START
-    inputType = InputType.TYPE_CLASS_TEXT or InputType.TYPE_TEXT_FLAG_MULTI_LINE
+    // PATCH: NO_SUGGESTIONS disables the spell-checker's red squiggly underline
+    // (e.g. under inserted mention handles like "@markchapman").
+    inputType = InputType.TYPE_CLASS_TEXT or
+      InputType.TYPE_TEXT_FLAG_MULTI_LINE or
+      InputType.TYPE_TEXT_FLAG_NO_SUGGESTIONS
 
     if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q) {
       breakStrategy = LineBreaker.BREAK_STRATEGY_HIGH_QUALITY
diff --git a/node_modules/react-native-enriched/android/src/main/java/com/swmansion/enriched/textinput/styles/ParametrizedStyles.kt b/node_modules/react-native-enriched/android/src/main/java/com/swmansion/enriched/textinput/styles/ParametrizedStyles.kt
index 44b68af..5852262 100644
--- a/node_modules/react-native-enriched/android/src/main/java/com/swmansion/enriched/textinput/styles/ParametrizedStyles.kt
+++ b/node_modules/react-native-enriched/android/src/main/java/com/swmansion/enriched/textinput/styles/ParametrizedStyles.kt
@@ -210,8 +210,33 @@ class ParametrizedStyles(
     val mentionRegex = Regex("^($indicatorsPattern)\\w*")
 
     val spans = spannable.getSpans(currentWord.start, currentWord.end, EnrichedInputMentionSpan::class.java)
-    for (span in spans) {
-      spannable.removeSpan(span)
+    // PATCH (atomic mentions): the edited word overlaps a finalized mention.
+    if (spans.isNotEmpty()) {
+      // Only delete the whole mention when the edit actually removed a character
+      // INSIDE the mention (flag set in the watcher's beforeTextChanged). An
+      // adjacent edit — removing the trailing space, or typing right next to the
+      // mention — leaves the mention intact; we just stop treating it as an
+      // in-progress mention so the suggestion list doesn't reopen.
+      val deletedMentionContent = view.mentionContentDeleted
+      view.mentionContentDeleted = false
+
+      if (deletedMentionContent) {
+        var delStart = Int.MAX_VALUE
+        var delEnd = Int.MIN_VALUE
+        for (span in spans) {
+          delStart = minOf(delStart, spannable.getSpanStart(span))
+          delEnd = maxOf(delEnd, spannable.getSpanEnd(span))
+          spannable.removeSpan(span)
+        }
+        if (delStart in 0..delEnd && delEnd <= spannable.length) {
+          view.runAsATransaction {
+            (spannable as Editable).delete(delStart, delEnd)
+          }
+        }
+      }
+
+      mentionHandler.endMention()
+      return
     }
 
     var indicator: String
@@ -234,6 +259,19 @@ class ParametrizedStyles(
         return
       }
 
+      // PATCH (atomic mentions): if the previous word is an already-FINALIZED
+      // mention (has a span), the user is typing AFTER a completed mention.
+      // Don't continue/reopen it.
+      val prevSpans = spannable.getSpans(
+        previousWord.start,
+        previousWord.end,
+        EnrichedInputMentionSpan::class.java,
+      )
+      if (prevSpans.isNotEmpty()) {
+        mentionHandler.endMention()
+        return
+      }
+
       // Previous word is a mention -> use it
       finalStart = previousWord.start
       indicator = mentionIndicatorRegex.find(previousWord.text)?.value ?: ""
@@ -323,6 +361,11 @@ class ParametrizedStyles(
       if (!hasSpaceAtTheEnd) {
         spannable.insert(safeEnd, " ")
       }
+
+      // PATCH: place the caret AFTER the trailing space so the next typed
+      // character does not touch the mention span (which would otherwise
+      // delete the mention or reopen the suggestion list).
+      view.setSelection(minOf(safeEnd + 1, spannable.length))
     }
 
     view.mentionHandler?.reset()
diff --git a/node_modules/react-native-enriched/android/src/main/java/com/swmansion/enriched/textinput/watchers/EnrichedTextWatcher.kt b/node_modules/react-native-enriched/android/src/main/java/com/swmansion/enriched/textinput/watchers/EnrichedTextWatcher.kt
index 028b41c..cb91815 100644
--- a/node_modules/react-native-enriched/android/src/main/java/com/swmansion/enriched/textinput/watchers/EnrichedTextWatcher.kt
+++ b/node_modules/react-native-enriched/android/src/main/java/com/swmansion/enriched/textinput/watchers/EnrichedTextWatcher.kt
@@ -1,11 +1,13 @@
 package com.swmansion.enriched.textinput.watchers
 
 import android.text.Editable
+import android.text.Spannable
 import android.text.TextWatcher
 import com.facebook.react.bridge.ReactContext
 import com.facebook.react.uimanager.UIManagerHelper
 import com.swmansion.enriched.textinput.EnrichedTextInputView
 import com.swmansion.enriched.textinput.events.OnChangeTextEvent
+import com.swmansion.enriched.textinput.spans.EnrichedInputMentionSpan
 
 class EnrichedTextWatcher(
   private val view: EnrichedTextInputView,
@@ -21,6 +23,22 @@ class EnrichedTextWatcher(
     after: Int,
   ) {
     previousTextLength = s?.length ?: 0
+
+    // PATCH (atomic mentions): record whether this edit deletes a character that
+    // is *inside* a mention span (the text shrinks within the mention). A
+    // deletion of an adjacent char (e.g. the trailing space) or an insertion
+    // leaves this false, so the mention is only nuked when its own text is hit.
+    val isDeletion = after < count && count > 0
+    view.mentionContentDeleted = if (isDeletion && !view.isDuringTransaction) {
+      val spannable = view.text as? Spannable
+      val end = (start + count).coerceAtMost(spannable?.length ?: 0)
+      spannable != null && start < end &&
+        spannable
+          .getSpans(start, end, EnrichedInputMentionSpan::class.java)
+          .isNotEmpty()
+    } else {
+      false
+    }
   }
 
   override fun onTextChanged(
diff --git a/node_modules/react-native-enriched/ios/EnrichedTextInputView.mm b/node_modules/react-native-enriched/ios/EnrichedTextInputView.mm
index 09045e5..933dd10 100644
--- a/node_modules/react-native-enriched/ios/EnrichedTextInputView.mm
+++ b/node_modules/react-native-enriched/ios/EnrichedTextInputView.mm
@@ -1840,7 +1840,8 @@ - (bool)textView:(UITextView *)textView
   // cause a crash so here I rely on short circuiting evaluation of the logical
   // expression either way it's not possible to have two of them come off at the
   // same time
-  if ([uStyle handleBackspaceInRange:range replacementText:text] ||
+  if ([mentionStyle handleMentionBackspace:range replacementText:text] ||
+      [uStyle handleBackspaceInRange:range replacementText:text] ||
       [uStyle tryHandlingListShorcutInRange:range replacementText:text] ||
       [oStyle handleBackspaceInRange:range replacementText:text] ||
       [oStyle tryHandlingListShorcutInRange:range replacementText:text] ||
diff --git a/node_modules/react-native-enriched/ios/interfaces/StyleHeaders.h b/node_modules/react-native-enriched/ios/interfaces/StyleHeaders.h
index 35aa9ee..4ab07cd 100644
--- a/node_modules/react-native-enriched/ios/interfaces/StyleHeaders.h
+++ b/node_modules/react-native-enriched/ios/interfaces/StyleHeaders.h
@@ -46,6 +46,8 @@
 - (void)manageMentionTypingAttributes;
 - (BOOL)handleLeadingMentionReplacement:(NSRange)range
                         replacementText:(NSString *)text;
+- (BOOL)handleMentionBackspace:(NSRange)range
+               replacementText:(NSString *)text;
 - (MentionParams *)getMentionParamsAt:(NSUInteger)location;
 - (NSRange)getFullMentionRangeAt:(NSUInteger)location;
 - (NSValue *)getActiveMentionRange;
diff --git a/node_modules/react-native-enriched/ios/styles/MentionStyle.mm b/node_modules/react-native-enriched/ios/styles/MentionStyle.mm
index 5d10772..10a4df4 100644
--- a/node_modules/react-native-enriched/ios/styles/MentionStyle.mm
+++ b/node_modules/react-native-enriched/ios/styles/MentionStyle.mm
@@ -482,6 +482,43 @@ - (BOOL)handleLeadingMentionReplacement:(NSRange)range
   return NO;
 }
 
+// PATCH (atomic mentions): when a single backspace would delete a character
+// that belongs to a finalized mention, remove the WHOLE mention instead of
+// editing it one letter at a time.
+- (BOOL)handleMentionBackspace:(NSRange)range
+               replacementText:(NSString *)text {
+  // Only a plain backspace: deleting exactly one character, no replacement.
+  if (text.length != 0 || range.length != 1) {
+    return NO;
+  }
+  NSUInteger location = range.location;
+  if (location >= _input->textView.textStorage.length) {
+    return NO;
+  }
+
+  // Only act when the deleted character itself belongs to a finalized mention.
+  // A character that merely sits next to a mention (e.g. the trailing space)
+  // is deleted normally — the mention goes only on the *next* backspace. This
+  // also lets the user type right next to a mention without removing it.
+  if ([self getMentionParamsAt:location] == nullptr) {
+    return NO;
+  }
+
+  NSRange mentionRange = [self getFullMentionRangeAt:location];
+  if (mentionRange.length == 0) {
+    return NO;
+  }
+
+  // Clear the mention attributes, then delete the whole mention range.
+  [self removeAttributes:mentionRange];
+  [TextInsertionUtils replaceText:@""
+                               at:mentionRange
+             additionalAttributes:nullptr
+                            input:_input
+                    withSelection:YES];
+  return YES;
+}
+
 // returns mention params if it exists
 - (MentionParams *)getMentionParamsAt:(NSUInteger)location {
   NSRange mentionRange = NSMakeRange(0, 0);
